
  # Create original homepage layout

  This is a code bundle for Create original homepage layout. The original project is available at https://www.figma.com/design/oF4GSdeYGihrzF8FGd4KRU/Create-original-homepage-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  